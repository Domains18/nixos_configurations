---
import ListPosts from '@/components/ListPosts'
import TitlePage from '@/components/TitlePage'
import Header from '@/components/Header'
import { getPosts } from '@/utils'

const MAX_POSTS = 5 // max number of posts to show on the home page
const posts = await getPosts(MAX_POSTS)
---

<div class="bg-white text-black bg-cover bg-center relative pb-4 px-5 md:px-0">

	<div class='absolute inset-0 bg-opacity-10'></div>
	<div class='sm:max-w-2xl md:max-w-screen-2xl mx-auto mt-4'>
		<TitlePage title='Latest Posts' classes='text-black mb-4' />
		<ListPosts FirstBig={true} posts={posts} classes='text-black' isIntro={true} />
	</div>
</div>

<script>
	import { animate } from 'motion'
	const showAnimations = localStorage.getItem('animations') === 'true'

	if (showAnimations) {
		animate(
			'.title',
			{ y: [80, 0], opacity: [0, 1] },
			{
				duration: 2.5,
				opacity: { duration: 3 },

				offset: [0, 0.55, 0.75]
			}
		)
	}
</script>
